#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include "main.h"

/**
 * _strcmp - a function
 * @s1: array of characters
 * @s2: array of characters
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int y = strcmp(s1, s2);

	return (y);
}
