#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * mul - Multiply two integers
 * @a: integer
 * @b: integer
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	int r = a * b;

	return (r);
}
