#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * add - Adds two numbers
 * @n: number
 * @m: number
 * Return: the result of summation
 */
int add(int n, int m)
{
        int r = n + m;

        return (r);
}
