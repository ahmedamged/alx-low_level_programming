#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 * _abs - returns absolute number
 * @n: number
 * Return: absolute number
 */
int _abs(int n)
{
	int r = abs(n);

	return (r);
}
